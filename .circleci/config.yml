version: 2.1

jobs:
  build:
    docker:
      - image: zaksabeast/devkit3ds-rust

    steps:
      - checkout
      - run:
          name: install
          command: rustup default nightly
      - run:
          name: make
          command: make debug

workflows:
  debug_build-workflow:
    jobs:
      - build
